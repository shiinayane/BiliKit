# BiliKit 测试方案

手动测试清单,覆盖 Float / Theme Sync / Wake Lock 三脚本及其协同。每条给出**步骤**与**理论应观察到的结果**;偏离即为缺陷。

## 0. 准备

- 浏览器:Safari(主目标)+ 任一 Chromium(Chrome/Edge)对照。
- 脚本管理器装齐三脚本:`float` / `theme-sync` / `wake-lock`(均启用)。
- 打开开发者工具,留意 Console 报错(理论上**全程无红色报错**)。
- 常用校验片段(控制台):
  - 主题:`document.documentElement.classList.contains('bili_dark')`
  - 抽屉 iframe 数:`document.querySelectorAll('.bfloat-iframe').length`
  - 预连接节点:`document.querySelectorAll('link[rel=preconnect]').length`
  - 页面 overflow:`getComputedStyle(document.body).overflow`

---

## 1. Float — 打开 / 点击拦截

| # | 步骤 | 预期结果 |
|---|------|----------|
|1.1|首页点一个视频卡|全屏面板**从右滑入**,iframe 内加载该视频页;底层首页不跳转|
|1.2|搜索页(search.bilibili.com)点视频|同 1.1(脚本 @match 覆盖 search)|
|1.3|`Cmd/Ctrl + 点击`视频卡|**不开抽屉**,浏览器照常新标签打开|
|1.4|中键点击视频卡|**不开抽屉**,新标签打开|
|1.5|点非视频链接(如 UP 主头像、分区)|**不拦截**,正常跳转|
|1.6|点番剧 `bangumi/play/...`|开抽屉(正则覆盖番剧)|

## 2. Float — 关闭 / 手势

| # | 步骤 | 预期结果 |
|---|------|----------|
|2.1|抽屉打开后按 `Esc`(未点进播放器)|抽屉关闭,面板向右滑出|
|2.2|**先点进播放器**再按 `Esc`|仍能关闭(keydown 已挂到 iframe 子窗口)|
|2.3|播放器进入**真全屏**后按 `Esc`|先退出全屏,**不关抽屉**;再按 `Esc` 才关|
|2.3a|抽屉评论输入框内按 `Esc`|不关闭抽屉，按键留给输入框/输入法|
|2.4|触控板**两指向右滑**(fullscreen/drawer-right)|面板**跟手横移**、背景同步渐隐;拖过约 20% 宽度松手→顺势滑出关闭，不足→回弹(其余形态为累计阈值即关)|
|2.5|点右下角 `✕`|抽屉关闭|
|2.6|浏览器后退键 / 触控板边缘后退|抽屉关闭,**不会**退到上一个历史页面|
|2.7|从 `www.bilibili.com` 首页打开抽屉，再在抽屉内点另一个视频|地址栏先变为打开的视频 URL，后续同步为新视频；不含 `#bk-drawer`|
|2.8|抽屉打开时后退，然后前进|后退关闭并恢复原 URL；前进重建抽屉及当前视频|
|2.9|从 `search.bilibili.com` / `space.bilibili.com` 打开 `www` 视频抽屉|因 History API 同源限制，地址栏保留原页；后退/前进仍能关闭/重开抽屉|
|2.10|`modal`/`drawer-*` 模式下点窗外暗区|抽屉关闭(全屏模式无暗区,此条跳过)|
|2.11|关闭后看 `document.querySelectorAll('.bk-dframe').length`|过渡与媒体清理结束(最迟~500ms)后为 **0**(iframe 销毁、停止后台播放)|
|2.12|连续快速打开/关闭抽屉，并交替用 Esc、关闭键、浏览器后退|每次都回到原页 URL，绝不跳到 `about:blank`；关闭后无声音/后台视频|

## 3. Float — 表现优化

| # | 步骤 | 预期结果 |
|---|------|----------|
|3.1|点开瞬间观察遮罩|先显示**模糊封面铺底 + 粉色转圈**,而非纯黑/白闪|
|3.2|懒加载未触发的卡片点开|无封面则回退纯色遮罩,不报错|
|3.3|加载完成|遮罩淡出,露出视频页;遮罩/底色深浅**跟随系统主题**|
|3.4|视频页加载后|**自动播放**(若浏览器自动播放策略拦截则停在暂停态,属正常,非 bug)|
|3.5|抽屉内 B 站顶栏|`#biliMainHeader`/`.bili-header` 被隐藏,播放器顶到最上|
|3.6|抽屉内广告位|`adSelectors` 命中的广告被隐藏|
|3.7|悬停视频卡 → 看 `link[rel=preconnect]` 数|出现 **6 个** preconnect 节点|
|3.8|12 秒内反复悬停多张卡|preconnect 节点**保持 6 个**(节流去重,不累积)|
|3.9|等待 >12s 再悬停|重新预连接(移除旧批、再建一批),仍为 6 个|

## 4. Float — 主题跟随

| # | 步骤 | 预期结果 |
|---|------|----------|
|4.1|系统深色下点开抽屉|抽屉内视频页为深色(cookie + 镜像一致)|
|4.2|**开着抽屉**时切换系统主题|抽屉内视频页**实时跟着翻**深/浅、**不刷新不断播**(观察器 → swap iframe 的 bili-theme 样式表 href)|
|4.3|关闭抽屉后控制台查 host `bili_dark`|抽屉关闭不影响宿主页主题;MutationObserver 已 disconnect|

## 5. Float — 新标签打开

| # | 步骤 | 预期结果 |
|---|------|----------|
|5.1|抽屉内视频播到 ~0:30,点右下 `↗`|新标签打开该视频,URL 带 `?t=30` 左右,从断点续播|
|5.2|点 `↗` 后原抽屉|**自动关闭**,且抽屉内视频已暂停(无双声)|
|5.3|`newTabResumeTime:false` 时点 `↗`|新标签打开但不带 `t=`,从头开始|

## 6. Float — 健壮性 / 回归

| # | 步骤 | 预期结果 |
|---|------|----------|
|6.1|开→关抽屉后,点首页任意处|**页面正常可点**(回归:遮罩 `pointer-events` 不残留)|
|6.2|开抽屉时 `getComputedStyle(document.body).overflow`|为 `hidden`(锁滚动)|
|6.3|关抽屉后再查 overflow|**恢复**为原值(非 `hidden`)|
|6.4|连续开→关→开多次|每次正常;无历史堆积(后退一次即回到开抽屉前)|
|6.5|在抽屉 iframe 内点一个相关视频|iframe **内部导航**到新视频(B 站原生行为),**不**嵌套开新抽屉(顶层守卫生效)|
|6.6|整个过程 Console|无红色报错|
|6.7|管理器里只装一份 BiliKit·Float(删掉旧 `Bilibili-Float`)|点视频只开**一个**抽屉;即便误装两份，单例守卫(`window.__BILIKIT_FLOAT__`)让后注入者退出，仍建议删旧版|

---

## 7. Theme Sync

| # | 步骤 | 预期结果 |
|---|------|----------|
|7.1|系统深色,新开 B 站标签|页面**初始即深色**,无浅→深闪烁(document-start 设 cookie + toggle)|
|7.2|页面打开状态下切换系统主题|当前页**无刷新**秒变;**页面不 reload**(关键回归点)|
|7.3|开多个 B 站标签,切系统主题|**所有标签**一起变,且**都不 reload**|
|7.4|查 cookie:`document.cookie.match(/theme_style=([^;]*)/)?.[1]`|深色为 `dark`、浅色为 `light`|
|7.5|后台搁置一个标签 → 切系统主题 → 切回该标签|该标签恢复可见时主题**已对**(visibilitychange 兜底)|
|7.6|抽屉 iframe 内查是否注入了 theme-sync 逻辑|theme-sync **不在 iframe 运行**(顶层守卫);抽屉主题由 Float 镜像负责|
|7.7|在**播放页**(`/video/*`)tab 切系统主题|也**无刷新**秒变(与首页同机制:swap `bili-theme` 样式表 href + toggle `bili_dark`/`night-mode`)|

## 8. Wake Lock(Safari,需电源/息屏设置允许)

| # | 步骤 | 预期结果 |
|---|------|----------|
|8.1|普通视频页播放|Console 出现 `[WakeLock] acquired`;系统不息屏|
|8.2|暂停视频|Console 出现 `[WakeLock] released`|
|8.3|切换分P / 播放器重建后继续播|重新绑定并再次 acquired(MutationObserver 跟踪)|
|8.4|标签切走再切回(播放中)|可见性恢复后重新 acquired|
|8.5|不支持 wakeLock 的环境|Console `[WakeLock] Not supported`,不报错|

---

## 9. 跨脚本协同

| # | 步骤 | 预期结果 |
|---|------|----------|
|9.1|**Float × Theme Sync**:开着抽屉时切系统主题|抽屉内视频实时翻主题(Float 镜像);抽屉**不被 reload**(theme-sync 顶层守卫不进 iframe)|
|9.2|**Float × Wake Lock**:抽屉内播放视频|抽屉 iframe 是 `video/*` 页,wake-lock 在其中生效 → 看视频也防息屏(`[WakeLock] acquired`)|
|9.3|三脚本同开,完整走一遍 1~8|互不报错、互不干扰|

## 10. 边界 / 异常

| # | 步骤 | 预期结果 |
|---|------|----------|
|10.1|禁用 cookie|theme-sync 当前页仍能直切(bili_dark),仅持久化失效;不报错|
|10.2|`prefers-color-scheme` 不支持的旧环境|theme-sync 退化为浅色,不报错|
|10.3|抽屉内导航到跨子域(space./t./m.)|净化/主题镜像等同源操作自动跳过(try/catch),不报错|
|10.4|快速连点多个视频卡|以最后一次为准,不残留多个抽屉/iframe|
|10.5|网络极慢、iframe `load` 迟迟不触发|加载遮罩 6 秒兜底自动撤下|

---

## 11. 评论信息

| # | 步骤 | 预期结果 |
|---|------|----------|
|11.1|打开含评论的视频，查看公开性别为男/女的评论|姓名右侧显示与头像悬停卡片一致的 16×16 B 站官方性别图标，无底色、间距 6px|
|11.2|查看性别为“保密”的评论|姓名右侧不显示性别图标|
|11.3|展开楼中楼、滚动加载更多评论|新评论同样补上图标，已有评论不重复注入|
|11.4|开启免登录模式查看评论|性别仍可显示；IP 属地因服务端匿名响应不返回而缺席|
|11.5|SPA 连续切换多个视频后观察 DOM/内存|旧评论树 observer 会断开，无随视频数单调累积|

---

## 已知非缺陷(预期内行为)

- **自动播放被拦** → 停在暂停态:浏览器自动播放策略所致,非 bug(3.4)。
- **隐藏顶栏可能留白/错位**:`headerSelectors` 依赖 B 站类名,B 站改版需手动调(3.5)。
- **横滑误关**:理论上 iframe 内长横向列表可能触发;跟手形态下面板会跟随、可反向回弹，误触概率低(2.4)。
- **主题跟系统而非 B 站站内开关**:设计如此(7.x)。
- **重复安装致双开**:旧 `Bilibili-Float` 未删时两份并存;单例守卫兜底，但请手动删旧版(6.7)。
- **抽屉是完整页**:比嵌入播放器重(高清/评论/弹幕换内存);曾试悬停预热整页提速因内存过高已回退。
